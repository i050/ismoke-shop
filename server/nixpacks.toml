# ====================================
# Nixpacks Configuration - Backend
# ====================================

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build || echo 'No build step'"]

[start]
cmd = "npm run start"
